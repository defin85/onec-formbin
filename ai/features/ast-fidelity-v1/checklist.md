# Checklist

- Does structural AST stability remain green on the managed fixtures?
- Does the CLI parse/build route still work on the managed fixtures?
- Were fidelity-specific goldens or metrics added before claiming fidelity improvements?
- Does the implementation avoid making stronger rebuild claims than the fixtures prove?
- Were docs updated if AST fidelity guarantees changed?
