# Checklist

- Does `inspect` still expose deterministic record metadata on the managed fixtures?
- Do `unpack` and `pack` still preserve byte-identical no-op roundtrip where expected?
- Do mirror-record edits remain packable while preserve-policy size changes stay rejected?
- Does pointer metadata remain correct for the verified split-form fixture?
- Were docs updated if codec guarantees, workflow, or support claims changed?
