# Checklist

- Does `semantic-form` produce the expected summary shape on the managed fixtures?
- Does the export move toward stable `semantic/` slices instead of a monolithic blob?
- Does the semantic-model layer stay compatible with split-form continuation handling?
- Does the implementation avoid making semantic parsing mandatory for raw-first repack safety?
- Does the feature avoid claiming full ordinary-form semantics in v1?
- Were docs updated if semantic guarantees or support claims changed?
