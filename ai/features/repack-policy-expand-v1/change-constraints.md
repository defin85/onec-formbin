# Change Constraints

- Keep the current mirror-safe edit behavior working while policy expansion is explored.
- Do not tune on `ai/features/repack-policy-expand-v1/holdout.jsonl`.
- Do not edit `tests/fixtures/*.Form.bin` during iteration.
- Keep preserve-policy size changes rejected unless a new fixture-backed rule proves otherwise.
- Add fixture evidence before broadening repack support claims.
- Update `README.md`, `docs/verification.md`, and `tests/fixtures/README.md` together when repack policy changes.
