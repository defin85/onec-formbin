# Change Constraints

- Keep the current three-fixture support matrix green while new variants are introduced.
- Do not tune on `ai/features/variant-support-matrix-v1/holdout.jsonl`.
- Do not move examples from holdout into development after seeing holdout behavior.
- Add fixture notes in `tests/fixtures/README.md` before broadening support claims.
- Keep raw-first safety guarantees unchanged unless new fixture evidence supports the change.
- Update `README.md`, `docs/verification.md`, and `tests/fixtures/README.md` together when support claims change.
